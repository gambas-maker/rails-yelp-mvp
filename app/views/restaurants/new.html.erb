<h2>Create your new restaurant la zone en personne</h2>
<%= simple_form_for (@restaurant) do |f| %>
<%= f.input :name %>
<%= f.input :address %>
<%= f.input :phone_number %>
<%= f.input :category, collection: Restaurant::CATEGORY %>
<%= f.submit %>
<% end  %>
